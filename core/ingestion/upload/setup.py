import setuptools

setuptools.setup(
    name = 'upload',

    packages = [
        'upload',
    ],

    install_requires = [
        'segyio',
        'numpy',
    ],

    setup_requires = [
    ],

    tests_require = [
    ],
)
