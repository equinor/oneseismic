version: '3.0'
services:
  core:
    image: lambdaville.azurecr.io/oneseismic-core:latest
    build:
      context: .
      dockerfile: core/Dockerfile

  api:
    depends_on:
      - core
    image: lambdaville.azurecr.io/api:latest
    build:
      context: api
