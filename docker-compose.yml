version: '3.0'
services:
  corestub:
    image: lambdaville.azurecr.io/corestub:latest
    build:
      context: api
      dockerfile: Dockerfile-corestub

  api:
    image: lambdaville.azurecr.io/api:${VERSION:-latest}
    build:
      context: api
